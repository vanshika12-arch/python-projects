# Phishing Detection Chatbot

This project is a Python chatbot that checks whether a URL may be a phishing link.

## Features
- Detects suspicious words like login, bank, verify
- Checks URL format
- Warns about possible phishing links

## How to run

Install library:
pip install validators

Run program:
python chatbot.py# python-projects
